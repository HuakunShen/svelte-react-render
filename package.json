{
  "name": "svelte-react-render",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm --filter api build && pnpm --filter app build",
    "build:api": "pnpm --filter api build",
    "preview": "pnpm --filter app preview",
    "check": "pnpm --filter app check"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
